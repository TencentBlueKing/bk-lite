## 1. 配置与预算基础

- [x] 1.1 新增五个环境变量的集中读取、默认值和正整数启动校验
- [x] 1.2 实现保守 token 估算接口，并明确 provider usage 优先、估算降级的计量来源
- [x] 1.3 实现 provider usage 采集、估算来源和预算值快照
- [ ] 1.4 增加缺失、合法、零、负数和非整数配置测试

## 2. 问答预算

- [x] 2.1 统一计算 Index、Overview、页面、图谱和来源摘要的知识 token
- [x] 2.2 提供请求级调用计数器与允许/拒绝门禁，由 Generation Navigation 负责高/低置信度业务路由
- [x] 2.3 为最终回答显式设置 1,500 token 默认输出上限
- [x] 2.4 返回 generation、used/estimated tokens、truncation 和 budget error
- [x] 2.5 与 Bot/ChatFlow 集成最终模型窗口检查且不裁剪当前问题
- [ ] 2.6 增加多知识来源、两次调用、输出截断和超限错误测试

## 3. 单文件构建预算

- [x] 3.1 为每个文件创建独立预算计数器并接入 BuildRecord
- [x] 3.2 按单次安全上下文自适应 Map，并在输出过大时分层 Reduce，禁止固定四块扩大 Prompt
- [x] 3.3 提供知识生成、必要冲突比较、可选语义 Overview 的预算优先级门禁，由构建编排决定实际业务步骤
- [x] 3.4 将累计 token 改为软阈值；保留调用次数、单次上下文和异常归并硬保护
- [x] 3.5 实现 budget_exhausted checkpoint、输入/结构/管线指纹与已完成 Map 输出保存；自动复用作为后续可选优化
- [ ] 3.6 增加批量文件隔离、软阈值继续、调用/上下文熔断、自适应分层 Reduce 和 stale checkpoint 测试

## 4. 可观测性与前端错误

- [x] 4.1 在 BuildRecord 和 Wiki Context 响应中记录预算 trace
- [x] 4.2 为问答超限、构建硬熔断和构建软阈值定义稳定状态与审计信息
- [x] 4.3 Web 提供大文件确认和 URL 用量提示，不提供用户扩额入口
- [x] 4.4 验证日志不记录完整问题、正文、Prompt 或凭据

## 5. 验收

- [ ] 5.1 验证普通高置信度问答只调用一次 LLM
- [ ] 5.2 验证问答知识 ≤8,000 token、输出 ≤1,500 token、调用 ≤2
- [ ] 5.3 验证普通文件少量调用、长文件自适应 Map/分层 Reduce、超过 60,000 token 仍继续且调用不超过硬熔断
- [ ] 5.4 验证超限构建不改变 active Generation
- [ ] 5.5 验证未配置使用默认值、非法配置启动失败
