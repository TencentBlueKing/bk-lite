os:
  linux: Linux
  windows: Windows
cloud_region:
  default:
    name: 默认
    description: 默认云区域
collector:
  jmx_activemq_linux:
    name: JMX ActiveMQ（Linux）
    description: 通过JMX收集ActiveMQ指标以监控消息队列性能和健康状况