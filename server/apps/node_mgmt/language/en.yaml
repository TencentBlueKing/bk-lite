cloud_region:
  default:
    description: Default Cloud Region
    name: Default
os:
  linux: Linux
  windows: Windows
collector:
  jmx_activemq_linux:
    description: Collects ActiveMQ metrics via JMX to monitor message queue performance
      and health
    name: JMX ActiveMQ (Linux)
