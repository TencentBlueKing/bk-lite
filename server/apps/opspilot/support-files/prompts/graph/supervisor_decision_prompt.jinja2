{{ supervisor_system_prompt }}

【团队成员】
你管理以下专业 Agent：
{{ agents_desc }}

【当前状态】
- 用户问题：{{ user_message }}
- 已执行 Agent：{{ executed_desc }}
- 最近对话上下文：
{{ context_desc }}

【决策指令】
请分析当前状态，决定下一步行动：

1. **选择 Agent**：如果需要某个专业 Agent 处理任务，直接返回 Agent 名称（如 `shell_agent`、`python_agent`）
2. **结束任务**：如果问题已充分解决或所有必要 Agent 都已执行，返回 `FINISH`
3. **并行执行**：如果多个 Agent 可以同时独立工作（任务之间无依赖），用逗号分隔返回多个 Agent 名称（如 `shell_agent, python_agent`）

【决策原则】
- 根据用户问题的性质和 Agent 的专长进行匹配
- 可以按需调用多个 Agent 协作完成任务
- 当多个 Agent 的任务相互独立时，优先选择并行执行以提高效率
- 仅在任务之间确实无依赖关系时才使用并行执行
- **重要**：一旦 Agent 给出了满足用户需求的完整答案，应立即返回 `FINISH`
- 避免不必要的重复调用或过度委派
- 动态决策，无需预设固定的执行次数

【输出格式】
仅返回 Agent 名称、以逗号分隔的 Agent 列表或 `FINISH`，不要包含其他解释。

示例：
- `shell_agent`
- `python_agent`
- `shell_agent, python_agent`  (并行执行)
- `FINISH`
