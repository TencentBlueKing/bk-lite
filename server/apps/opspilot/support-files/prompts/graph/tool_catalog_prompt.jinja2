## Available Tool Categories

You have access to `activate_tools` which lets you load additional tools when needed.

Before using a tool category, you must first activate it by calling:
```
activate_tools(categories="category_name")
```

You can activate multiple categories at once:
```
activate_tools(categories="kubernetes,mysql")
```

Review the available categories and activate only what you need for the current task.
