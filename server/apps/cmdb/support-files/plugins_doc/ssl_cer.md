### 说明
对已录入的 SSL 证书实例域名做 TLS 握手，采集颁发者、颁发时间和截止时间，写回同一条实例。不新建、不删除证书资产，也不改实例名和域名。

### 前置
1. 先录入实例（实例名 + 域名），再创建采集任务。
2. 采集任务只选实例，不能选网段。
3. 默认端口 443，无凭据。
4. 过期或自签仍采集成功（不校验证书链）。

### 操作入口
CMDB → 管理 → 自动发现 → 采集 → 专业采集 → SSL 证书。
