[[inputs.prometheus]]
    startup_error_behavior = "retry"
    urls = ["${STARGAZER_URL}/api/monitor/redfish/metrics"]
    interval = "{{ interval | default(60, true) }}s"
    timeout = "{{ timeout | default(60, true) }}s"
    response_timeout = "{{ response_timeout | default(60, true) }}s"

    [inputs.prometheus.http_headers]
        host = "{{ ip }}"
        username = "{{ username }}"
        password = "${PASSWORD__{{ config_id }}}"
        port = "{{ port | default(443, true) }}"
        verify_tls = "{{ verify_tls | default(true) | lower }}"
        instance_id = "{{ instance_id }}"
        instance_type = "{{ instance_type }}"
        collect_type = "redfish"
        config_type = "hardware_server"
