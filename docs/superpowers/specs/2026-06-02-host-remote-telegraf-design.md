# Host Remote 与旧 Telegraf 主机场景兼容设计

## 背景

当前主机对象接入链路已支持新增 `Host Remote` 插件，但在“同一主机实例已存在旧 Telegraf 采集插件，再新增另一种采集方式”时会报错：

`渲染采集模板失败：type=host, instance_id=('MTVmOTFiYTM5ODZk',)`

本次设计聚焦 `POST /monitor/api/node_mgmt/batch_setting_node_child_config/` 这条接入链路，目标是在不破坏旧数据和现有接口入参的前提下，修复**主机对象**中“同一实例已存在旧 Telegraf 采集，再新增 `Host Remote`”的失败场景。

## 问题陈述

现有实现把 `instance_id` 同时用于三类职责：

1. 接口入参中的外部实例标识。
2. `MonitorInstance` / `CollectConfig` / `MonitorObjectOrganizationRule` 等模型之间的持久化关联键。
3. 模板渲染时使用的实例变量。

这三类职责当前没有明确边界，导致：

- 实例复用、配置冲突判断、模板渲染分别依赖 `instance_id` 的不同表现形式。
- 旧链路遗留的实例记录已经存在并可被复用，但新增第二种采集方式时，后续配置创建与模板渲染阶段仍可能错误地消费持久化键格式。
- 报错点虽然发生在模板渲染阶段，但根因更接近“实例识别契约和模板变量契约没有明确分离”。

## 目标

1. 保持前端现有请求格式不变，继续接受类似 `"instance_id": "MTVmOTFiYTM5ODZk"` 的入参。
2. 不迁移、不破坏旧数据；历史实例和已有关联关系继续有效。
3. 同一主机实例允许在已存在旧 Telegraf 主机采集时继续新增 `Host Remote`（`collector=Telegraf`, `collect_type=http`）。
4. 同一实例下，相同采集器、相同采集方式、相同配置类型的重复创建仍然应被拦截。
5. 模板渲染错误需要能区分“实例解析失败”“重复配置冲突”“模板字段缺失”等场景，避免继续输出过于笼统的错误。
6. 不改变内置插件与页面自定义插件的模板来源选择逻辑。

## 非目标

- 不修改前端接口协议。
- 不做全量数据迁移。
- 不重构整个监控对象实例建模方式。
- 不引入新的全局共存语义；仅修复已确认命中的主机对象场景。
- 不借此次 bugfix 改动内置插件与非内置（页面自定义）插件的模板来源选择逻辑。

## 现状契约梳理

本次修复不预设“所有模型统一使用同一种 ID 格式”，而是沿现有代码契约梳理每一层：

### 接口入参层

- `batch_setting_node_child_config` 接收来自前端的 `instance_id`。
- 真实请求中，`instance_id` 为业务侧原始实例标识，例如 `"MTVmOTFiYTM5ODZk"`。

### 持久化关联层

- `MonitorInstance.id` 是实例主键。
- `CollectConfig.monitor_instance` 通过外键依赖 `MonitorInstance.id`。
- `MonitorObjectOrganizationRule.monitor_instance_id` 以字符串形式关联监控实例。
- 当前创建链路会在服务层对实例 ID 做一次旧式转换，并基于转换后的结果执行“是否已存在”“是否复用实例”“是否重建组织关系”等逻辑。

### 模板渲染层

- `Controller.render_template` 需要一个适合模板使用的实例变量。
- 模板渲染真正需要的是“模板语义上的实例值”，而不是未经区分的持久化键表现形式。

## 推荐方案

在不改变旧数据和外部接口的前提下，引入一套**限定在主机对象接入链路内的边界适配规则**：

1. **入参解析统一化**：请求进入服务层后，先把 `instance_id` 解析成稳定的“实例语义值”。
2. **持久化键适配显式化**：凡是需要查 `MonitorInstance`、复用实例、关联 `CollectConfig`、更新 `MonitorObjectOrganizationRule` 的地方，都通过同一组适配函数获取当前持久化层真正使用的键，而不是在各处手写转换。
3. **模板变量显式化**：模板渲染时使用明确的模板实例变量，而不是直接复用持久化键。
4. **重复配置判断保持现有语义**：配置冲突判断仍以“同一实例 + 同一采集器 + 同一采集方式 + 同一配置类型”为准，不扩大也不缩小现有拦截面。
5. **模板来源逻辑不变**：内置插件与非内置插件仍沿现有 `monitor_plugin_id` 分支选择模板来源。

## 方案对比

### 方案 A：只给 Host Remote 做特判

- 在 `Host Remote` / `collect_type=http` 场景下单独修补 `instance_id` 和模板变量。
- 优点：改动小。
- 缺点：后续其它“同实例新增不同采集方式”仍可能重复踩坑。

### 方案 B：限定在主机对象已有实例复用链路内的边界适配（推荐）

- 把实例识别逻辑收敛到服务层的统一适配函数中。
- 复用实例、配置查重、模板渲染分别消费明确职责的值。
- 仅覆盖主机对象中“已存在实例 + 新增不同采集方式”的链路，不借机改写其它插件的模板选择逻辑。
- 优点：能修当前问题，同时把影响面控制在已命中的场景里。
- 缺点：对其它潜在同类问题先保持观察，不在本次一并处理。

### 方案 C：重做实例身份建模

- 彻底拆开实例业务标识、持久化键、模板渲染变量。
- 优点：长期最干净。
- 缺点：范围过大，不适合本次 bugfix。

## 详细设计

### 1. 实例 ID 处理原则

在主机对象接入链路中新增统一的实例 ID 处理助手，输出三类值：

1. **raw_input**：接口原始入参，用于日志和诊断。
2. **logical_instance_value**：表达“这是哪个业务实例”的稳定值，供模板变量和业务判断使用。
3. **storage_instance_key**：当前持久化层查库和关联真正使用的键，供 `MonitorInstance` / `CollectConfig` / `MonitorObjectOrganizationRule` 相关逻辑使用。

这三类值必须由同一个入口计算，业务代码不再在不同函数里分别拼装和猜测格式。

### 2. 服务层改造点

#### `create_monitor_instance_by_node_mgmt`

- 在实例进入 `_prepare_instances_for_creation` 前，先完成实例 ID 解析和边界适配。
- 后续服务逻辑携带结构化的实例标识信息，而不是只传一个未经区分的 `instance_id` 字符串。
- 此适配只用于本次确认受影响的主机对象接入路径，不改变其它未命中问题的业务入口。

#### `_prepare_instances_for_creation`

- 使用 `storage_instance_key` 查找 `MonitorInstance` 是否存在。
- 使用同一 `storage_instance_key` 检查 `CollectConfig` 冲突。
- 若仅是新增不同采集方式，则复用实例并允许继续生成配置。
- 若为同一采集方式下的重复配置，则明确返回“重复配置冲突”。

#### `_create_instances_in_db`

- 对已存在实例只执行复用和属性同步，不创建重复实例。
- 组织关系与默认规则继续沿现有逻辑处理，但使用统一的持久化键来源。

### 3. 模板渲染改造点

#### `Controller.controller`

- 接收已经完成实例标识适配的数据，不再依赖上游传入的 `instance_id` 恰好是某种字符串表现形式。
- 模板来源选择逻辑保持现状：有 `monitor_plugin_id` 时按插件模板走，无 `monitor_plugin_id` 时按内置模板走。

#### `Controller.render_template`

- 模板渲染优先使用 `logical_instance_value` 或一个显式模板变量。
- 若实例解析失败，报错应明确指向实例解析，而不是统一归为模板渲染失败。
- 保留对旧输入形态的兼容，但兼容逻辑集中在实例 ID 处理助手，而不是散落在模板层。

### 4. 错误处理

将当前过于笼统的报错拆分为更具体的失败类型：

- **实例识别失败**：接口入参与当前持久化契约无法映射。
- **重复配置冲突**：同一实例下重复创建相同采集器/采集方式/config_type。
- **模板变量不足或模板渲染失败**：模板自身所需变量缺失或模板内容异常。

对外仍使用业务异常返回，但日志中需携带：

- `raw_input.instance_id`
- 解析后的 `logical_instance_value`
- 参与查库的 `storage_instance_key`
- `collector`
- `collect_type`
- `config_type`

## 数据兼容策略

1. 旧表数据不迁移。
2. 历史实例和配置关联保持有效。
3. 新逻辑通过统一适配函数理解旧数据契约，而不是改写旧记录。
4. 不假设所有模型都应该切换到某一种统一 ID 格式；每个边界继续消费它本来依赖的值，但值的来源必须统一。
5. 内置插件与页面自定义插件继续沿用现有模板来源选择逻辑。

## 测试设计

### 回归测试

构造一个已存在旧主机采集配置的实例，再提交如下类型的新请求：

- `collector = Telegraf`
- `collect_type = http`
- `config.type = host`
- `instance_id = "MTVmOTFiYTM5ODZk"`

断言：

- 接口成功返回。
- 不创建重复 `MonitorInstance`。
- 新增一组新的 `CollectConfig`。
- 原有旧采集配置保持不变。

### 兼容测试

同一业务实例分别用以下输入触发流程：

1. 原始字符串实例 ID。
2. 旧式字符串化实例键。

断言：

- 两种输入都能命中同一持久化实例。
- 都能正确执行“复用已有实例 + 新增不同采集方式”。

### 非内置插件保护测试

选取一个页面自定义创建、当前能正常接入的插件场景。

断言：

- 模板仍按现有 `monitor_plugin_id` 分支加载。
- 本次修复不改变其原有成功路径与报错行为。

### 冲突测试

对同一实例重复提交相同 `collector + collect_type + config_type`。

断言：

- 返回明确的重复配置冲突错误。
- 不新增配置。

### 模板变量测试

验证模板渲染使用的是模板真正需要的实例变量，而不是直接使用持久化键。

断言：

- 在旧数据存在时渲染成功。
- 当实例变量不可解析时返回“实例识别失败”或更明确的模板输入错误。

## 实施边界

本次实现预计主要落在：

- `server/apps/monitor/services/node_mgmt.py`
- `server/apps/monitor/utils/plugin_controller.py`
- `server/apps/monitor/utils/dimension.py`（如适合承载统一实例 ID 适配逻辑）
- 对应 `server/apps/monitor/tests/` 回归测试

## 验收标准

1. 已存在旧主机采集插件的实例，可以继续新增 `Host Remote`。
2. 不迁移旧数据，不破坏旧实例与配置关联。
3. 同一采集方式的重复配置仍然被拦截。
4. 内置插件/页面自定义插件的模板来源选择逻辑保持不变。
5. 报错日志能区分实例识别、重复配置冲突、模板渲染失败三类问题。
