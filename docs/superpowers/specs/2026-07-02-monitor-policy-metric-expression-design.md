# 监控策略指标表达式产品设计

## 1. 产品判断

本次扩展不应把 Monitor 告警策略拆成“单指标 / 多指标 / PromQL”三种用户模式，而应把现有 `指标` 字段升级为一个综合的指标表达式编辑器。

推荐方案：

- 用户始终在配置 `指标`。
- 单指标只是指标编辑器里保留一行指标的形态。
- 多指标通过 `a`、`b`、`c` 等指标行变量组成公式，产出一个最终结果序列。
- 不向普通策略创建页暴露 PromQL 输入框；PromQL/MetricsQL 只作为后端查询构建结果和已有计算指标的底层实现。
- 多指标公式必须提供 `结果名称`，用于告警名称模板中的 `metric_name` 变量、预览展示和事件记录。

这个方向更接近 Datadog 的 Query + Formula 心智，但不照搬其完整查询语言。BK-Lite 的指标本身已经是基于 PromQL/MetricsQL 的计算指标，页面再开放一层 PromQL 会让用户面对两套表达能力，增加学习成本和反向解析难题。第一阶段应把能力收敛在可视化指标行、结构化过滤、分组聚合和受限公式表达式上。

## 2. 背景与现状

仓库事实：

- 后端策略模型 `MonitorPolicy.query_condition` 是 JSON 字段，具备扩展空间。
- 后端已有 `query_condition.type = "metric"` 和 `query_condition.type = "pmq"` 的基础校验。
- 当前普通策略创建页以单个指标、单套过滤维度和单套分组维度为中心。
- 策略预览已经通过 `/monitor/api/monitor_policy/preview/` 走后端查询构建，适合继续作为统一预览入口。
- 后台扫描链路以最终查询结果做阈值判断、触发、恢复和无数据判断。

已确认产品决策：

- 页面不新增外层“查询模式”。
- 字段名称仍叫 `指标`，不改成 `查询`。
- `指标` 是一个综合交互框，内部完成指标选择、条件过滤、分组聚合和公式配置。
- 不再设计用户可编辑 PromQL 面板。
- 分组维度和条件维度不再作为 `指标` 外部的独立字段暴露。
- 聚合周期和聚合方式仍属于策略评估配置，不属于单个指标行的外层字段。

当前问题：

1. 现有单指标模型无法表达错误率、使用率差值、容量余量、流量合计等跨指标告警。
2. 如果直接让用户写 PromQL，会绕开已有指标目录、维度选择、单位、权限和预览体验。
3. 如果只保存拼接后的查询字符串，后续编辑时无法稳定还原指标、条件、分组和公式。
4. 从单指标升级到多指标后，告警模板变量 `metric_name` 缺少明确来源。

## 3. 设计目标

- 保留旧单指标策略行为，不要求历史策略迁移。
- 将现有单指标交互自然扩展为多指标表达式，不引入单独的“单指标模式”。
- 支持新增、删除多个指标行，并用受限公式生成最终评估结果。
- 在每个指标行内配置条件维度和分组维度。
- 条件维度使用结构化控件：维度、对比方式、值；支持多个条件。
- 多个条件从第二条开始支持选择 `AND` / `OR`。
- 多指标时提供 `结果名称`，作为最终指标名称。
- 预览、保存、扫描、阈值判断和无数据告警都面向最终结果序列。
- 服务端在 API 边界校验公式、变量、条件和分组一致性，避免后台扫描崩溃。

## 4. 非目标

- 不实现用户可编辑 PromQL 面板。
- 不实现完整 Datadog 查询语言。
- 不允许页面输入任意脚本、自定义函数或任意查询片段。
- 不做 PromQL 反向解析为可视化配置。
- 不改变阈值等级、通知、策略启停、任务调度和告警生命周期语义。
- 不在第一阶段实现子查询曲线调试、公式模板市场或查询成本治理。

## 5. 用户体验设计

### 5.1 定义指标步骤

`定义指标` 步骤仍展示字段 `指标`。该字段内部是指标表达式编辑器。

编辑器由三部分组成：

| 区域 | 说明 |
| --- | --- |
| 指标行 | 选择一个或多个指标，并配置每行的过滤、分组聚合和分组维度 |
| 公式行 | 当存在两个及以上指标行时展示，输入结果名称和表达式 |
| 预览区 | 展示最终结果序列、单位、最终评估查询和 warning |

单指标策略只显示一个指标行，不显示公式行。多指标策略显示多个指标行和一条公式行。

### 5.2 指标行

每个指标行包含：

- 变量名：系统分配 `a`、`b`、`c`，用于公式引用。
- 指标：从指标目录中选择。
- 条件：0 个或多个结构化条件。
- 分组聚合：例如 `avg by`、`sum by`、`max by`。
- 分组维度：例如 `instance_id`、`service`。
- 删除按钮：当指标行数量大于 1 时展示。

指标行内容允许自动换行，不要求所有控件挤在一行。换行后仍应保持变量名、指标选择、条件、分组和删除操作的视觉归属清晰。

### 5.3 条件维度

条件维度不使用整段文本输入，而使用结构化控件：

| 字段 | 控件 | 示例 |
| --- | --- | --- |
| 维度 | 下拉选择 | `service`、`instance_id` |
| 对比方式 | 下拉选择 | `=`、`!=`、`=~`、`!~` |
| 值 | 输入框或可搜索选择 | `checkout`、`host-.*` |
| 连接符 | 从第二个条件开始展示 | `AND`、`OR` |

交互规则：

- 第一个条件前不显示连接符。
- 从第二个条件开始显示 `AND` / `OR` 选择。
- 用户可以新增和删除条件。
- 条件为空时该指标行不加过滤。
- 保存前校验维度名、对比方式和值均合法。

查询构建规则：

- 全部为 `AND` 时，构建为同一个指标选择器的多个 label matcher。
- 出现 `OR` 时，按条件组合构建多个分支，再用 MetricsQL/PromQL 的 `or` 合并分支。
- 第一阶段不暴露括号分组 UI；条件按页面顺序从左到右构建。
- 如果后端暂无法安全支持混合 `AND` / `OR`，必须在保存时给出明确错误，不允许静默降级。

### 5.4 分组聚合

分组配置集成在每个指标行内。

每行支持：

- 分组聚合方式：`avg`、`sum`、`min`、`max` 等。
- 分组维度：多选。

多指标公式的第一阶段规则：

- 所有参与公式的指标行必须聚合到相同的 `group_by` 维度集合。
- 各行可以选择不同聚合方法，但保存时必须校验 `group_by` 一致。
- 不在 UI 暴露 `on()`、`ignoring()`、`group_left`、`group_right` 等高级向量匹配能力。

### 5.5 公式行

当指标行数量大于 1 时展示公式行。

公式行结构：

```text
结果名称 = 表达式
```

示例：

```text
HTTP 5xx 错误率 = a / b * 100
```

字段规则：

- `结果名称` 必填，用于最终指标展示、事件记录和模板变量 `metric_name`。
- `结果名称` 建议限制为 1 到 64 个字符。
- `表达式` 必填，只能引用已存在的指标行变量。
- 第一阶段公式白名单只支持 `+`、`-`、`*`、`/`、括号和数字常量。
- 后续如需要 `clamp_min`、`abs` 等函数，应独立扩展白名单和测试。

删除规则：

- 用户可以删除指标行，但至少保留一行。
- 删除被公式引用的指标行后，前端应立即提示公式失效，并阻止保存。
- 如果只剩一个指标行，公式行隐藏，公式草稿可保留在前端内存中；再次添加第二行时可恢复，保存时以当前可见状态为准。

### 5.6 策略级评估配置

以下字段仍在 `指标` 编辑器之外，作为策略评估配置：

- 聚合周期。
- 聚合方式。
- 阈值等级。
- 连续触发、恢复和无数据配置。

聚合周期不是指标行配置，也不是分组维度配置。系统先根据指标行和公式生成基础结果序列，再按策略级聚合周期与聚合方式进行最终评估。

## 6. 数据契约

### 6.1 单指标策略

旧单指标策略继续保存为 `type = "metric"`，降低兼容成本。

```json
{
  "type": "metric",
  "metric_id": 123,
  "filter": [
    {
      "connector": "AND",
      "name": "service",
      "method": "=",
      "value": "checkout"
    }
  ],
  "group_algorithm": "avg",
  "group_by": ["instance_id"]
}
```

说明：

- 单指标 UI 仍使用新的指标表达式编辑器，只是落库保持旧结构。
- `connector` 对旧数据可选；旧数据没有该字段时按 `AND` 处理。
- 如果历史字段名与实现不一致，以现有后端字段为准，新增字段需做兼容读取。

### 6.2 多指标公式策略

新增 `type = "formula"`。

```json
{
  "type": "formula",
  "result_name": "HTTP 5xx 错误率",
  "queries": [
    {
      "ref": "a",
      "type": "metric",
      "metric_id": 123,
      "filter": [
        {
          "connector": "AND",
          "name": "status",
          "method": "=~",
          "value": "5.."
        }
      ],
      "group_algorithm": "sum",
      "group_by": ["instance_id"]
    },
    {
      "ref": "b",
      "type": "metric",
      "metric_id": 456,
      "filter": [],
      "group_algorithm": "sum",
      "group_by": ["instance_id"]
    }
  ],
  "formula": "a / b * 100"
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `type` | string | 固定为 `formula` |
| `result_name` | string | 最终结果名称，多指标必填 |
| `queries` | list | 参与公式的指标行 |
| `queries[].ref` | string | 变量名，唯一，第一阶段由系统分配 |
| `queries[].metric_id` | number | 指标 ID |
| `queries[].filter` | list | 结构化条件维度 |
| `queries[].group_algorithm` | string | 指标行分组聚合方法 |
| `queries[].group_by` | list | 指标行分组维度 |
| `formula` | string | 最终表达式 |

第一阶段 `queries[].type` 只允许 `metric`。不允许公式子查询嵌套自由查询片段。

### 6.3 历史 PMQ 策略

已有 `type = "pmq"` 策略保留兼容：

- 后端继续支持读取、扫描和预览。
- 普通策略创建页不提供新建 PMQ 的用户入口。
- 编辑历史 PMQ 策略时，第一阶段可采用只读提示或保留原有专项入口，不能强行反向转成指标表达式。

这部分属于兼容边界，不是本次产品体验的主路径。

## 7. 后端设计

### 7.1 统一查询构建服务

新增或重构统一服务，例如：

```text
PolicyQueryBuilder
```

职责：

- 接收策略草稿或 `MonitorPolicy` 实例。
- 根据 `query_condition.type` 解析 `metric`、`formula` 和历史 `pmq`。
- 生成基础 MetricsQL 查询。
- 生成最终评估 MetricsQL 查询。
- 返回指标元数据、单位、最终结果名称、实例标识键和 warning。

现有预览和扫描不能各自拼查询。`PolicyPreviewService`、后台扫描链路和无数据判断应复用同一套构建逻辑。

### 7.2 单指标构建

单指标构建规则：

1. 读取 `Metric.query`。
2. 将结构化条件转成 label matcher 或 `or` 分支。
3. 替换指标查询模板中的 label 占位。
4. 使用该指标行的 `group_algorithm` 和 `group_by` 聚合。
5. 使用策略级 `algorithm` 和 `period` 包装为最终评估查询。
6. 最终结果名称使用指标显示名。

### 7.3 多指标公式构建

公式构建规则：

1. 每个指标行先按单指标规则构建基础聚合查询。
2. 校验所有指标行的 `group_by` 集合一致。
3. 校验公式只引用已声明变量。
4. 将变量替换为对应子查询。
5. 对公式结果应用策略级 `algorithm` 和 `period`。
6. 最终结果名称使用 `result_name`。

示例：

```json
{
  "result_name": "HTTP 5xx 错误率",
  "formula": "a / b * 100",
  "queries": [
    {"ref": "a", "metric_id": 1},
    {"ref": "b", "metric_id": 2}
  ]
}
```

最终查询形态示例：

```promql
avg_over_time((((sum(metric_a) by (instance_id)) / (sum(metric_b) by (instance_id)) * 100)[5m:10s])
```

运行时规则：

- 当公式结果为 `NaN`、`+Inf`、`-Inf` 时，阈值判断跳过该点并记录 warning。
- 单个策略查询失败不影响其他策略扫描。
- 查询错误需要返回可行动信息，例如“公式引用了不存在的变量 c”。

### 7.4 条件 OR 构建

条件构建需要明确支持 `AND` 和 `OR`。

示例条件：

```text
service = checkout AND env = prod OR env = staging
```

第一阶段按从左到右的简单表达式处理，可构建为：

```promql
metric{service="checkout", env="prod"} or metric{env="staging"}
```

如果该语义不能满足复杂条件分组，后续再设计条件组 UI。第一阶段不提供括号和嵌套条件组。

## 8. 告警事件与模板变量

阈值判断仍只面向最终评估结果。

事件字段建议：

- `MonitorEvent.value` 保存最终结果数值。
- `metric_instance_id` 继续由最终结果的实例标识维度构建。
- `dimensions` 保存最终结果序列维度。
- `metric_name` 的取值规则：
  - 单指标：使用所选指标显示名。
  - 多指标公式：使用 `result_name`。
  - 历史 PMQ：使用已有策略保存的名称；如不存在则使用“自定义指标结果”。

告警名称模板中的 `${metric_name}` 必须始终有确定值。多指标策略保存时如果 `result_name` 为空，应阻止保存。

## 9. 无数据告警

无数据告警基于最终评估结果判断。

- 单指标策略保持现有逻辑。
- 多指标公式策略只有当最终公式结果缺失时触发无数据。
- 不对每个子查询单独触发无数据告警。
- 子查询缺失导致公式无结果时，可在预览和策略日志中记录 warning。
- 策略作用范围为空时继续跳过扫描，不产生误告警。

## 10. API 与校验

### 10.1 序列化校验

`MonitorPolicySerializer.validate_query_condition` 需要扩展：

- `type` 必须是 `metric`、`formula` 或历史兼容 `pmq`。
- `metric` 必须包含合法 `metric_id`。
- `formula` 必须包含至少两个子查询。
- `formula.result_name` 必填。
- `formula.formula` 必填。
- `formula.queries[].ref` 唯一，符合 `^[a-z][a-z0-9_]*$`。
- `formula` 只能引用已声明变量。
- `formula` 不允许出现白名单之外的字符、函数和变量。
- 所有条件维度必须通过 label name、method 和 value 校验。
- 公式策略所有 `group_by` 集合必须一致。

### 10.2 预览接口

继续使用：

```text
POST /monitor/api/monitor_policy/preview/
```

请求体支持 `metric` 和 `formula`。

响应建议：

```json
{
  "query": "<final-evaluation-query>",
  "result_name": "HTTP 5xx 错误率",
  "data": {
    "status": "success",
    "data": {
      "result": []
    },
    "unit": "%"
  },
  "warnings": []
}
```

`query` 是后端生成的最终评估查询，仅用于调试和问题排查。前端不应自行拼最终查询。

## 11. 前端实现边界

策略创建页从单个 `metric` 状态升级为 `metricExpressionDraft`。

```ts
interface MetricConditionDraft {
  connector?: 'AND' | 'OR';
  dimension: string;
  method: '=' | '!=' | '=~' | '!~';
  value: string;
}

interface MetricLineDraft {
  ref: string;
  metricId?: number;
  metricName?: string;
  groupAlgorithm: string;
  groupBy: string[];
  conditions: MetricConditionDraft[];
}

interface MetricExpressionDraft {
  lines: MetricLineDraft[];
  formula?: string;
  resultName?: string;
}
```

保存转换规则：

- `lines.length === 1`：保存为 `type = "metric"`。
- `lines.length > 1`：保存为 `type = "formula"`，并要求 `formula` 和 `resultName` 非空。

单位规则：

- 单指标：沿用指标单位自动填充。
- 多指标公式：默认不继承单位，用户手动选择；如果公式是同单位加减，后续可做单位推荐。

错误提示：

- 公式引用不存在的变量。
- 删除指标后公式失效。
- 多指标未填写结果名称。
- 多指标分组维度不一致。
- 条件维度或条件值非法。

## 12. 权限、审计与可靠性

- 策略创建、更新仍走现有策略权限，不新增独立权限。
- 服务端继续按组织、监控对象、数据源和策略作用范围做二次校验。
- 创建、更新、启停、删除策略的审计语义不变。
- 后台扫描遇到非法查询、VictoriaMetrics 错误或非有限结果时，本轮策略失败应记录日志并跳过，不影响其他策略。
- 预览失败应向用户展示可行动错误，不只展示通用失败。

## 13. 兼容与迁移

- 旧 `type = "metric"` 策略不迁移，编辑时回填为一行指标。
- 新建单指标策略保存后仍可使用旧 `metric` 结构。
- 新建多指标策略保存为 `formula`。
- 旧 `type = "pmq"` 策略不迁移；继续保证扫描兼容。
- 旧策略模板不强制升级；模板导入仍可导入单指标模板。
- 后续如要支持公式模板，只需在模板 JSON 中增加 `query_condition.type = "formula"`。

## 14. 验收场景

### 14.1 单指标兼容

- 新建 CPU 使用率策略，只保留一行指标，预览、保存、扫描、触发和恢复行为与当前一致。
- 编辑旧单指标策略，回填为一行指标，保存后不丢失原有过滤和分组字段。

### 14.2 多指标表达式

- 创建 `HTTP 5xx 错误率 = a / b * 100` 策略，两个指标按相同 `group_by` 聚合后产生最终序列。
- 创建 `a + b` 策略，两个同维度指标合并后可正常预览和触发。
- 删除被公式引用的指标行后，前端提示公式失效并阻止保存。
- 多指标未填写结果名称时阻止保存。
- 后端拒绝引用未定义变量的公式，例如 `a + c`。
- 当除数为 0 产生非有限值时，后台任务不崩溃。

### 14.3 条件维度

- 一个指标行配置多个 `AND` 条件，生成正确过滤查询。
- 一个指标行配置 `OR` 条件，生成多个分支并合并。
- 条件行可新增、删除，第一行不展示连接符，第二行开始展示 `AND` / `OR`。
- 非法维度名、非法比较方式或空值被阻止保存。

### 14.4 模板变量

- 单指标策略中 `${metric_name}` 渲染为所选指标名称。
- 多指标策略中 `${metric_name}` 渲染为 `result_name`。
- 历史 PMQ 策略中 `${metric_name}` 有兼容默认值。

### 14.5 无数据告警

- 多指标公式最终结果缺失时触发无数据。
- 子查询单独缺失但最终结果仍有数据时，不额外触发子查询无数据告警。
- 策略作用范围为空时继续跳过扫描，不产生误告警。

### 14.6 测试门禁

后端测试：

- `MonitorPolicySerializer` 覆盖 `metric`、`formula` 和历史 `pmq` 校验。
- 查询构建服务覆盖单指标、公式和条件 `AND` / `OR`。
- `PolicyPreviewService` 覆盖公式预览。
- 扫描链路覆盖公式结果阈值判断。
- 非法公式、查询错误和非有限值不导致扫描器崩溃。

前端测试：

- 一行指标保存为 `metric` payload。
- 多行指标保存为 `formula` payload。
- 条件维度新增、删除、AND/OR 切换。
- 公式引用校验、删除保护和结果名称必填。
- 预览调用后端 preview，不在前端拼最终查询。

## 15. 实施建议

建议分三步实现：

1. 后端查询构建和校验：先支持 `formula` 保存、预览、查询生成和错误返回。
2. 后台扫描链路接入：让公式进入真实阈值判断、事件生成和无数据告警。
3. 前端指标编辑器升级：将现有 `指标` 字段升级为综合表达式编辑器，并接入预览。

第一步完成后可通过接口测试验证核心语义；第二步完成后形成真实告警闭环；第三步完成后对用户可见。

## 16. 后置能力

- 子查询曲线预览和公式调试面板。
- 公式函数扩展，例如 `clamp_min`、`clamp_max`、`abs`。
- 条件组括号和嵌套逻辑。
- 公式模板导入和推荐。
- 查询成本预估、超时控制和策略级查询资源配额。
- 面向高级用户的独立查询实验室；该能力不进入普通策略创建主流程。

## 17. 待确认项

- 历史 `pmq` 策略在普通策略编辑页的具体呈现：只读提示、保留旧入口，还是单独高级编辑页。
- 多指标公式的单位是否必须由用户手动选择，还是允许后端基于公式和源指标做推荐。
- 条件 `OR` 的第一阶段语义是否接受从左到右构建，还是需要直接设计条件组。
