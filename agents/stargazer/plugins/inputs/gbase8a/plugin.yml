name: gbase8a
version: "1.0.0"
category: database
metadata:
  display_name: "GBase 8a"
  description: "GBase 8a 数据库协议采集插件（MySQL 兼容）"
  author: "WeOps"
  tags: [database, gbase8a]
  cloud_protocol: false
  model_id: gbase8a
executors:
  protocol:
    type: protocol
    timeout: 30
    collector:
      module: plugins.inputs.gbase8a.gbase8a_info
      class: Gbase8aInfo
default_executor: protocol
