# Change specs

每个 material、跨会话变更使用一个目录和一份 `spec.md`。状态为 `draft | ready | in-progress | done | cancelled`；完成后原地更新，不 archive。

只有工作超出一个上下文窗口或存在真实阻塞边时才创建 `tickets/<NN>-<slug>.md`。完整规则见 `docs/agents/issue-tracker.md`。
