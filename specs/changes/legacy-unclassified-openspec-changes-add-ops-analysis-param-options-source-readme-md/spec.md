# add-ops-analysis-param-options-source

Status: cancelled

## Migration Context

非标准旧 artifact，原路径为 `openspec/changes/add-ops-analysis-param-options-source/README.md`。完整内容保留如下。

统一运营分析参数输入配置：支持输入框、下拉选择、单选按钮；下拉/单选支持自定义选项或数据源选项。组件参数和统一筛选共用同一个编辑器，内置数据源可通过 `sourceRef/rest_api` 声明默认动态选项来源。
