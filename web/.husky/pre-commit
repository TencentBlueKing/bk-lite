#!/bin/sh
cd "$(dirname "$0")/.." || exit 1  # Critical line: change to web directory

# First generate tsconfig.lint.json
npm run precommit

# Run lint check (type-check disabled temporarily due to Next.js 16 + React 19 upgrade)
npm run lint & npm run type-check   
# TODO: Fix type errors and re-enable: npm run type-check
