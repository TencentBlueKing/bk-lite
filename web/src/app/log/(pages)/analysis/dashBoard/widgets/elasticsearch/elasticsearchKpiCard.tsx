import React from 'react';
import ComKpiCard from '../comKpiCard';

const ElasticsearchKpiCard: React.FC<any> = (props) => {
  return <ComKpiCard {...props} />;
};

export default ElasticsearchKpiCard;
