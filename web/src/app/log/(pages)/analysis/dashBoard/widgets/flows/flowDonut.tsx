import React from 'react';
import DockerDonutChart from '../docker/dockerDonutChart';

const FlowDonut: React.FC<any> = (props) => {
  return <DockerDonutChart {...props} />;
};

export default FlowDonut;
