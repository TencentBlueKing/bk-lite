import React from 'react';
import ComKpiCard from '../comKpiCard';

const WindowsEventKpiCard: React.FC<any> = (props) => {
  return <ComKpiCard {...props} />;
};

export default WindowsEventKpiCard;
