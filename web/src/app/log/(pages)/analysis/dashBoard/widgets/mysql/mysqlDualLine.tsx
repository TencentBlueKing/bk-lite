import React from 'react';
import DockerDualLine from '../docker/dockerDualLine';

const MysqlDualLine: React.FC<any> = (props) => {
  return <DockerDualLine {...props} />;
};

export default MysqlDualLine;
