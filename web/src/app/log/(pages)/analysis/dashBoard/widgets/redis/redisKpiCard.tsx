import React from 'react';
import DockerKpiCard from '../docker/dockerKpiCard';

const RedisKpiCard: React.FC<any> = (props) => {
  return <DockerKpiCard {...props} />;
};

export default RedisKpiCard;
