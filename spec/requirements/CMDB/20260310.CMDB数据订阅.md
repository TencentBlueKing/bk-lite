# CMDB数据订阅

日期：2026-03-10

## 背景

当前 CMDB 对实例数据变化缺少主动通知能力，负责人通常依赖人工巡检或被动发现，存在以下问题：

- 关键配置被变更后无法及时感知（例如物理服务器内存配置变化）。
- 关联资源变化难以及时追踪（例如关联磁盘容量变化）。
- 时间类字段接近到期时缺少提前提醒（例如维保时间临近）。

为提升 PLG 场景下的自助体验与可用性，CMDB 需要提供“数据订阅”能力：用户可按模型与规则订阅实例数据变化，并通过系统管理已有通知渠道接收提醒。

## 需求描述

CMDB 新增“数据订阅”能力，支持在当前组织范围内订阅实例数据变化并发送通知。

### 1）订阅入口与范围

- 在资产页面新增“数据订阅”按钮，点击后以侧边弹框展示当前用户可见的订阅规则列表。
- 订阅规则的创建、查看、编辑、启停、删除均在该侧边弹框内完成。
- 订阅规则作用范围限定为当前组织，不包含子组织。
- 规则新增“所属组织”字段用于权限控制：仅当规则所属组织与当前用户组织匹配时，当前用户才可管理该规则。

Nano Banana 提示词：

- 设计一个 CMDB 资产页面，页面右上角有“数据订阅”主按钮。点击后右侧滑出抽屉，标题为“数据订阅规则”。抽屉内展示规则列表（规则名、所属组织、目标模型、状态、最近触发时间、操作列）。操作列包含查看、编辑、启停、删除。列表顶部有“新建规则”按钮和搜索框。页面风格企业级、信息密度中等、操作清晰。
- 在同一个原型中补充权限态：当用户组织与规则所属组织不一致时，编辑、启停、删除按钮置灰并显示 tooltip“仅所属组织可管理”。

### 2）快速订阅入口

提供以下四个快速订阅入口，减少用户配置成本：

- 实例列表多选后提供“订阅”按钮，点击后默认选择已勾选实例；规则名称默认 `{模型}{当前时间戳}`；通知对象默认当前用户；所属组织默认当前组织。
- 实例列表筛选后提供“订阅”按钮，点击后默认按当前筛选条件选择实例；规则名称默认 `{模型}{当前时间戳}`；通知对象默认当前用户；所属组织默认当前组织。
- 实例详情页提供“订阅”按钮，点击后默认选择当前实例；规则名称默认 `{实例名称}{当前时间戳}`；通知对象默认当前用户；所属组织默认当前组织。
- 资产页面“数据订阅”侧边弹框提供完整规则管理入口（增删改查）。

Nano Banana 提示词：

- 设计四个“快速订阅”入口联动原型：1）实例列表多选后显示“订阅”按钮；2）实例列表筛选后显示“订阅”按钮；3）实例详情页显示“订阅”按钮；4）从“数据订阅规则”抽屉点击“新建规则”。所有入口打开同一个“新建订阅规则”抽屉，但默认值不同：多选入口默认已选实例；筛选入口默认按当前筛选条件选实例；详情入口默认当前实例；规则名称默认“模型或实例名+时间戳”；通知对象默认当前用户；所属组织默认当前组织。请把默认值以高亮标签形式展示，便于用户理解。

### 3）支持的触发类型

订阅规则支持以下触发类型：

- 属性变化：目标模型实例指定字段值发生变化时触发。
- 关联变化：目标模型实例一跳关联的另一模型实例数据发生变化时触发。
- 临近到期：目标模型时间类型字段值在“到期前 N 天”范围内时触发（按自然日口径）。

Nano Banana 提示词：

- 设计“触发类型”配置区，使用单选卡片组件展示三种类型：属性变化、关联变化（一跳）、临近到期。切换卡片时右侧动态出现对应配置表单：属性变化显示字段多选；关联变化显示关联模型下拉+关联字段多选；临近到期显示时间字段下拉+提前天数输入框并标注“自然日”。在同一画布给出三种切换后的状态对比。

### 4）规则配置要求

创建/编辑订阅规则时需支持以下配置：

- 规则名称（必填）。
- 所属组织（必填，默认当前组织，用于规则管理权限控制）。
- 目标模型（必选）。
- 选择实例（必选）：
  - 支持逐个勾选实例。
  - 支持按搜索条件筛选实例。
  - 搜索条件最多支持 8 个，多条件之间为“且（AND）”逻辑。
- 触发类型（必选，支持上述三类）。
- 触发条件：
  - 属性变化：可选择监听字段。
  - 关联变化：可选择一跳关联模型与监听字段。
  - 临近到期：可设置提前天数（正整数）。
- 接收对象（至少一个，用户或用户组）。
- 通知渠道（必选，使用系统管理已有通知渠道）。
- 规则状态（启用/停用）。


### 5）通知发送与防打扰

- 触发后按规则向接收对象发送通知。
- 通知需包含：模型名称、实例标识、触发类型、变化摘要（或到期信息）、触发时间。
- 内置防打扰聚合策略：同一规则在 1 分钟窗口内的同类触发进行聚合发送，避免通知风暴。


### 6）实例数据校验与变更口径

- 属性变化与关联变化按 CMDB 当前已生效数据变化事实触发。
- 临近到期提醒按规则当前绑定字段与当前实例值计算。
- 公共选项库、枚举、多选等字段在本需求中按现有 CMDB 字段行为处理，不单独改变其业务语义。


### 7）规则生效与历史数据

- 订阅规则保存后仅对后续触发生效，不追补历史变更通知。
- 临近到期规则在启用后按调度周期执行扫描并触发。
- 历史实例值本身不因订阅能力而被修改。


### 8）文案与按钮（中英文对照）

以下文案用于原型与国际化基线，英文采用简洁企业产品风格。

按钮与入口：

- 数据订阅 / Data Subscription
- 订阅 / Subscribe
- 新建规则 / New Rule
- 查看 / View
- 编辑 / Edit
- 启用 / Enable
- 停用 / Disable
- 删除 / Delete
- 保存并启用 / Save & Enable
- 仅保存 / Save Draft
- 取消 / Cancel
- 搜索 / Search
- 重置 / Reset

规则与表单字段：

- 规则名称 / Rule Name
- 所属组织 / Owning Organization
- 目标模型 / Target Model
- 选择实例 / Select Instances
- 条件筛选 / Filter Conditions
- 最多 8 个条件 / Up to 8 Conditions
- 且（AND） / AND
- 触发类型 / Trigger Type
- 触发条件 / Trigger Conditions
- 属性变化 / Attribute Change
- 关联变化（一跳） / Related Model Change (1-hop)
- 临近到期 / Expiration Approaching
- 提前天数 / Days Before Expiration
- 接收对象 / Recipients
- 通知渠道 / Notification Channel
- 规则状态 / Rule Status

列表与详情：

- 订阅规则列表 / Subscription Rules
- 最近触发时间 / Last Triggered At
- 执行信息 / Execution Info
- 上次扫描时间 / Last Scan Time
- 下次扫描时间 / Next Scan Time
- 最近一次触发结果 / Latest Trigger Result

状态与提示：

- 规则已保存并生效 / Rule has been saved and activated
- 仅所属组织可管理 / Only users in the owning organization can manage this rule
- 同一规则同类事件 1 分钟内自动聚合为一条通知 / Events of the same type under one rule are aggregated within 1 minute into one notification
- 仅对后续触发生效，不追补历史通知 / Effective for future events only; historical events are not backfilled
- 历史实例值不会被修改 / Historical instance values will not be modified
- 至少选择一个接收对象 / Select at least one recipient
- 请选择通知渠道 / Please select a notification channel
- 请填写规则名称 / Please enter a rule name

默认值说明文案：

- 默认通知对象：当前用户 / Default recipient: Current user
- 默认所属组织：当前组织 / Default organization: Current organization
- 默认规则名称：{模型}{当前时间戳} / Default rule name: {Model}{Current Timestamp}
- 默认规则名称：{实例名称}{当前时间戳} / Default rule name: {Instance Name}{Current Timestamp}

## 适用范围

- CMDB：资产页面“数据订阅”侧边弹框与快速订阅入口。
- CMDB：数据订阅规则管理（创建、查看、编辑、启停、删除）。
- CMDB：实例变化触发与通知发送。
- 系统管理：复用已有通知渠道进行消息投递。

## 非目标（本期不包含）

- 跨组织/跨租户订阅。
- 多跳关联链路订阅（仅支持一跳关联）。
- 新增通知渠道类型（仅复用系统管理已有渠道）。
- 复杂流程能力（如审批流、版本管理、订阅模板市场）。

## 验收标准

- 资产页面存在“数据订阅”按钮，点击后可在侧边弹框查看并管理当前用户可见规则。
- 规则作用范围仅为当前组织，其他组织不可见不可用。
- 规则包含“所属组织”字段，且仅所属组织匹配当前用户时可管理。
- 提供四个快速订阅入口：实例列表多选、实例列表筛选、实例详情、管理侧边弹框。
- 快速订阅入口默认值生效：实例范围、规则名称、通知对象、所属组织符合预期。
- 规则支持三类触发：属性变化、关联变化（一跳）、临近到期（按自然日）。
- 规则配置项完整可用：名称、所属组织、目标模型、选择实例、触发类型与条件、接收对象、通知渠道、启停状态。
- 选择实例支持逐个勾选与条件筛选；条件数量最多 8 个且按 AND 逻辑生效。
- 触发后可通过系统管理已有通知渠道向接收对象发送通知。
- 通知内容可识别触发来源，至少包含模型、实例、触发类型、摘要、时间。
- 同一规则在 1 分钟窗口内的同类触发会聚合发送，不产生明显重复轰炸。
- 停用规则后不再触发通知，重新启用后恢复触发。
- 规则仅对创建后触发生效，不追补历史变更通知。
